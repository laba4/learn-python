services:
  json-server:
    container_name: mock-api
    build: 
      context: .
      dockerfile: .docker/Containerfile
    volumes:
      - ./api:/app/api
    networks:
      - backend
    ports:
      - "3000:3000"
    command: npm run server

networks:
  backend: